name: Pull Request Checks

on:
  pull_request:
    branches: [master]

jobs:
  dotnet-test-coverage:
    uses: ./.github/workflows/jobs/dotnet-test-coverage.yaml

  website-build:
    uses: ./.github/workflows/jobs/website-build.yaml
